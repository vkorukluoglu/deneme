import { AdminPageShell } from "@/components/layout/AdminPageShell";import { SectionCard } from "@/components/shared/SectionCard";import { FilterBar } from "@/components/shared/FilterBar";import { DataTable } from "@/components/shared/DataTable";import { StatusBadge } from "@/components/shared/StatusBadge";import { AppTabs } from "@/components/shared/AppTabs";import { DetailHeader } from "@/components/shared/DetailHeader";import { KPIStatCard } from "@/components/shared/KPIStatCard";import { StickyActionBar } from "@/components/shared/StickyActionBar";import { FormSection } from "@/components/shared/FormSection";import { FileUploadPlaceholder } from "@/components/shared/FileUploadPlaceholder";import { SummarySidebar } from "@/components/shared/SummarySidebar";import { ActivityFeed } from "@/components/shared/ActivityFeed";import { offers,proformas,serviceRequests,partStocks,machines,customers,parts,partRequests,stockMovements,workOrders,calendarEvents,portalRequests,portalMachines } from "@/data/mock";import { OfferWizard } from "@/components/sales/OfferWizard";import { StockSummaryCard } from "@/components/parts/StockSummaryCard";import { WarrantyCard } from "@/components/service/WarrantyCard";
export default function Page(){return <AdminPageShell title="Dashboard"><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><KPIStatCard label="Açık Teklif" value={offers.length}/><KPIStatCard label="Açık Servis" value={serviceRequests.length}/><KPIStatCard label="Proforma" value={proformas.length}/><KPIStatCard label="Kritik Stok" value={partStocks.length}/></div><div className="grid gap-4 lg:grid-cols-2"><SectionCard title="Son Proformalar"><DataTable headers={["No","Durum"]} rows={proformas.map(p=>[p.no,<StatusBadge key={p.id} status={p.durum}/>])}/></SectionCard><SectionCard title="Son Teklifler"><DataTable headers={["No","Durum"]} rows={offers.map(o=>[o.no,<StatusBadge key={o.id} status={o.durum}/>])}/></SectionCard></div><div className="grid gap-4 lg:grid-cols-3"><SectionCard title="Son Servis Talepleri"><DataTable headers={["Talep","Durum"]} rows={serviceRequests.map(s=>[s.talepNo,<StatusBadge key={s.id} status={s.durum}/>])}/></SectionCard><SectionCard title="Kritik Stok Uyarıları"><DataTable headers={["Parça","Kullanılabilir"]} rows={partStocks.map((s,i)=>["Parça-"+(i+1),String(s.mevcut-s.rezerve)])}/></SectionCard><SectionCard title="Son Aktiviteler"><ActivityFeed items={["Yeni teklif açıldı","Servis talebi geldi","Stok güncellendi"]}/></SectionCard></div></AdminPageShell>;}