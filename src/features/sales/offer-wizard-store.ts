"use client";import { create } from "zustand";type SalesType="Leasing"|"Nakit"|"Kredi";interface S{step:number;salesType:SalesType;cost:number;margin:number;setField:<K extends keyof S>(k:K,v:S[K])=>void;next:()=>void;prev:()=>void;}export const useOfferWizardStore=create<S>((set)=>({step:1,salesType:"Nakit",cost:8000000,margin:0.1,setField:(k,v)=>set((s)=>{const n={...s,[k]:v} as S;if(k==="salesType")n.margin=v==="Leasing"?0.01:0.1;return n;}),next:()=>set(s=>({step:Math.min(6,s.step+1)})),prev:()=>set(s=>({step:Math.max(1,s.step-1)}))}));